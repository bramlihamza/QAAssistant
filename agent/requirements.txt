# ⚠️  Ce fichier est conservé pour compatibilité (Docker, CI sans uv).
# La source de vérité est pyproject.toml — géré par uv.
#
# Pour régénérer :
#   uv export --no-hashes --no-dev -o requirements.txt
#
# Pour installer avec pip (hors uv) :
#   pip install -r requirements.txt

langchain>=0.3.25
langchain-openai>=0.3.18
langchain-community>=0.3.24
langchain-chroma>=0.2.4
openai>=1.82.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.11.0
chromadb>=1.0.9
tiktoken>=0.9.0
ragas>=0.2.15
datasets>=3.6.0
python-dotenv>=1.1.0
requests>=2.32.0
